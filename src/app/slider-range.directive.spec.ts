import { SliderRangeDirective } from './slider-range.directive';

describe('SliderRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderRangeDirective();
    expect(directive).toBeTruthy();
  });
});
